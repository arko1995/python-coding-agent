[project]
name = "python-coding-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "arko1995", email = "shahriarsohelarko@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
python-coding-agent = "python_coding_agent:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
